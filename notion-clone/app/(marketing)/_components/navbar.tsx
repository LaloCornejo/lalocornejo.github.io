"use client";

import { useScrollTop } from "@/hooks/use-scroll-top";

export const Navbar =() => {
    cosnt scroled = useScrollTop
    return(
        <div className={cn(
            "z-50 "
        )}>
            
        </div>
    );
}